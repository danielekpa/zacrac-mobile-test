import { formStyleObj } from '../types/types';

const formStyle: formStyleObj = {
  labelStyle: 'absolute text-black left-4 top-[-10px] z-10 font-bold text-sm tracking-wide bg-white px-1 py-[1px]',
  inputStyle:
  'border-icon border pl-4 py-3 font-medium leading-4 text-black tracking-wide',
};

export default formStyle;
